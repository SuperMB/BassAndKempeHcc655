// Write your Javascript code.

function MouseOverCursor(ev, id) {
    document.getElementById(id).style.cursor = "pointer";
}

function MouseOutDefault(ev, id) {
    document.getElementById(id).style.cursor = "default";
}